import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#667788',
    },
    background: {
      default: '#ffffff',
      paper: '#eeeeee',
    },
    text: {
      primary: '#345',
      secondary: '#535',
      disabled: '#66696f',
    },
  },
});
